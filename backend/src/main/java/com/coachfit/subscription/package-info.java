@ApplicationModule(
        id = "subscription",
        displayName = "Subscription",
        allowedDependencies = { "shared" }
)
package com.coachfit.subscription;

import org.springframework.modulith.ApplicationModule;
