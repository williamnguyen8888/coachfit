@ApplicationModule(
        id = "coach",
        displayName = "Coach",
        allowedDependencies = { "shared" }
)
package com.coachfit.coach;

import org.springframework.modulith.ApplicationModule;
