@ApplicationModule(
        id = "analytics",
        displayName = "Analytics",
        allowedDependencies = { "shared" }
)
package com.coachfit.analytics;

import org.springframework.modulith.ApplicationModule;
