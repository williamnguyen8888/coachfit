/**
 * Named interface: exposes auth domain model types (AuthUser, RefreshToken)
 * to modules that need to work with auth domain objects.
 */
@NamedInterface("auth-domain")
package com.coachfit.auth.domain.model;

import org.springframework.modulith.NamedInterface;
