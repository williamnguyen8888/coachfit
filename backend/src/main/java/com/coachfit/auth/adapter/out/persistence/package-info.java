/**
 * Auth persistence adapters package.
 *
 * <p>Named interface exposing {@code AesTokenEncryptionUtil} to the {@code sync} module
 * (used by Strava token refresh).
 */
@NamedInterface("auth-adapters")
package com.coachfit.auth.adapter.out.persistence;

import org.springframework.modulith.NamedInterface;
