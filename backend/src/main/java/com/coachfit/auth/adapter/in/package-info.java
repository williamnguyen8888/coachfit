/**
 * Inbound adapters for the auth module.
 *
 * <p>Named interface exposing OAuth provider properties (e.g. {@code StravaOAuthProperties})
 * to the {@code sync} module.
 */
@NamedInterface("auth-config")
package com.coachfit.auth.adapter.in;

import org.springframework.modulith.NamedInterface;
